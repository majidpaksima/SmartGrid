# هنداف (Handoff) — نسخه ۲ (smart_grid_bot_v2)

پوشه `D:\Grid1\smart_grid_bot_v2` یک کپی از ورژن ۱ (کامیت `d019cfc` روی `main`) است که رفتار «بستن سیکل» در آن بازطراحی شد. کد پایه کاملاً همان v1 است و فقط تغییرات زیر اعمال شده.

## تغییر کلیدی v2: سیکل فقط وقتی کامل میشود که تیپی همه پوزیشنها زده شود
در v1 سیکل وقتی بسته میشد که **هر** پوزیشن شناختهشده ناپدید میشد (`_check_target_hit`) — یعنی یک تیپیِ محلیِ کوچکِ کاشت (ورود+۱گام) میتوانست کل سیکل را زودتر ببندد (بگ موردی که در لاگ سیکل ۶ دیده شد).

در v2:
- سیکل فقط زمانی به CLOSING میرود که **همه** پوزیشنهای باز شده در این سیکل بسته شده باشند (تیپی برای همه زده شده باشد).
- در این لحظه `_handle_closing` سفارشهای پندینگ باقیمانده را لغو میکند و سیکل ریست میشود.
- بستهشدن تکپوزیشنیِ اتفاقی دیگر سیکل را نمیبندد.

### فایلهای تغییر یافته
- `models/symbol_context.py` — فیلد جدید `position_tickets_seen: set` (مجموعه تیکتهایی که در این سیکل باز شدهاند).
- `strategy/cycle_manager.py`:
  - `_refresh_state_from_mt5` — تیکت پوزیشنهای باز را به `position_tickets_seen` اضافه میکند.
  - `_handle_resetting` — `position_tickets_seen` را پاک میکند.
  - `_check_all_positions_closed(ctx, sc)` — جدید: `True` فقط وقتی `position_tickets_seen` غیرخالی باشد و فعلاً هیچ پوزیشن بازی نباشد.
  - `_handle_positions_active` / `_handle_target_active` / `_handle_locked_exposure` — بهجای `_check_target_hit` از `_check_all_positions_closed` استفاده میکنند؛ `previous_tickets`/`previous_trigger` حذف شد.
  - `_handle_grid_active` — بستنِ `_all_grids_filled` حذف شد (سیکل فقط با تیپی کامل میشود).
  - متدهای `_check_target_hit` و `_all_grids_filled` حذف شدند.

## تغییر بعدی v2: حذف تیپیِ کاشت (±۱ گام) از سفارشهای معلق
از تحلیل لاگ سیکل ۴ v2 (XAUUSD): تیپیِ ±۱ گام که پلن v1 (کامیت `d019cfc`) روی سفارشهای معلق میگذاشت باعث میشد پوزیشنِ پر شده تقریباً بلافاصله تیپی شود و ناپدید شود. سمت فروش بهطور متوالی مصرف و محو میشد (فقط استاپهای خرید باقی میماندند) و سبد هم نمیتوانست SL هج را بچسباند (retcode 10013 — تداخل با همان تیپیِ نزدیک).

تغییر اعمالشده:
- `strategy/grid_builder.py` — در `build_orders` و `build_orders_for_depth` مقدار `tp` برای هر دو سمت روی `0.0` قرار گرفت (هیچ تیپی کاشت روی سفارش معلق نیست).
- نتیجه: سفارش پر شده بهعنوان پوزیشنِ سبد میماند؛ پوزیشن همجهت تیپی سبد میگیرد و پوزیشن هج (سمت مخالف) SL سبد را میگیرد — بدون تداخل تیپیِ نزدیک.
- تستها بهروز شدند: `test_build_orders_for_depth_has_no_tp`، `test_build_orders_has_no_tp`، `test_place_grid_depths_passes_no_tp`.

## تغییر بعدی v2: فریز هدف سبد در حرکت یکطرفه + بازنگری با پوزیشن خلافجهت
از تحلیل لاگ سیکل ۵ v2 (XAUUSD): هدف سبد هر تیک از صفر بازمحاسبه میشد (`_try_set_basket_target` → `calculate_basket_target`) و تیپی پوزیشنهای باقیمانده همزمان با تیپیِ چند پوزیشن تغییر میکرد. خریدهای باز هدفِ متغییری میگرفتند (4175.74 ← 4175.35 ← 4177.2 ← … ← 4178.65) و آخرین خرید C5_20 (ورود 4178.45) تیپی 4178.65 بالای بازار گرفت، هیچوقت پر نشد، ۵ استاپ فروش معلق ماندند و سیکل با بستن دستی تمام شد. علت: `target_calculator.py` از قیمت لحظهای شروع براکت را میجوید، سودِ محققساخته را نادیده میگیرد و `_set_basket_targets` هر تیک تیپی/اسالِ همهٔ پوزیشنها را بازنویسی میکرد.

طبق تصمیم کاربر: «وقتی به یک سمت حرکت داریم هدف فریز شود؛ فقط وقتی پوزیشن برعکس باز شد تیپیِ کل معاملات بازنگری شود».

تغییر اعمالشده:
- `models/symbol_context.py` — فیلدهای جدید:
  - `basket_target_locked: bool` — آیا هدف فریز شده است.
  - `basket_direction: Optional[int]` — جهت غالب سبد هنگام قفل (1=خرید، -1=فروش).
  - `locked_position_tickets: set` — تیکتهای باز هنگام قفل (برای تشخیص تیکت خلافجهت جدید).
  - `settled_position_tickets: set` — تیکتهای بستهشده که سودشان حساب شده (جلوگیری از شمارش دوباره).
  - `realized_net_profit: float` — مموریِ سود خالصِ محققساختهٔ سیکل (از تاریخچهٔ MT5) برای لاگ/ریپورت.
- `strategy/cycle_manager.py`:
  - `_try_set_basket_target` — بار اول هدف را محاسبه، روی گرید نگه میدارد و قفل میکند. هر تیک بعدی، تا وقتی `_should_recompute_target` درست نشده، فقط همان `target_price` فریز را روی همهٔ پوزیشنها اعمال میکند (هیچ بازمحاسبه و تیپی فراری).
  - `_should_recompute_target` — جدید: `True` فقط وقتی تیکت جدیدِ پوزیشنِ خلافجهتِ قفل باز شود یا جهت نتی سبد بچرخد. بستهشدنِ پوزیشنِ همراستا یا پر شدنِ پوزیشنِ همراستای جدید قفل را برنمیدارد.
  - `_basket_direction` — جدید: جهت غالب بر اساس حجم (1 اگر حجم خرید ≥ فروش، وگرنه -1).
  - `_set_basket_targets` — وقتی قفل فعال است، جهتِ نقش TP↔SL را از `basket_direction` میگیرد تا با پر شدنِ تیکت خلافجهت، نقشها لحظهای نچرخند.
  - `_refresh_state_from_mt5` — تیکتهای ناپدیدشده را از طریق `history_service.get_deals_for_position` (دیال ورودیِ 1=خروج) حساب کرده و به `realized_net_profit` اضافه میکند.
  - `_handle_resetting` — فیلدهای قفل و مموری سود را پاک میکند و P/L محققساختهٔ سیکل را لاگ میکند.
- تستهای جدید (۶ عدد): فریز با بستهشدنِ همراستا، فریز با پر شدنِ همراستای جدید، بازنگری با باز شدنِ پوزیشن خلافجهت، چرخشِ جهت با هجِ بزرگتر، پاکسازی در ریست، و جمعِ یکبارِ `realized_net_profit`.

## تغییر بعدی v2: تیپیِ یکسان برای همهٔ پوزیشنهای همجهت
از تحلیل سیکل ۱ آخرین اجرا (۱۰:۲۶–۱۰:۳۱، anchor=4177.30): سه خرید آخر C1_18/19/20 (ورود 4179.48/4179.60/4179.71) تیپیهای غیریکسان گرفتند (4179.77/4179.77/4179.86) و تا بستن دستیِ اضطراری باز ماندند. علت: «فالبکِ تیپیِ محلی» در `_set_basket_targets` — هر پوزیشنِ همجهت که `entry ± step` از هدف مشترک فراتر میرفت، تیپیِ جداگانهٔ `entry+step` میگرفت (C1_20 → 4179.86).

تغییر اعمالشده:
- `strategy/cycle_manager.py` — `_set_basket_targets`:
  - فالبکِ تیپیِ محلی حذف شد؛ همهٔ پوزیشنهای همجهت دقیقاً یک تیپیِ مشترک میگیرند.
  - هدفِ مشترک قبل از اعمال به «فراتر از عمیقترین ورودِ همجهت» کلمپ میشود: خرید غالب → `max(target, max_entry_buy + step)`، فروش غالب → `min(target, min_entry_sell − step)`، سپس `_align_to_tick`.
  - SL هج (سمت مخالف) بدون تغییر.
- `utils/logger.py` — باگِ قورت دادنِ `message` وقتی فیلد هست برطرف شد؛ حالا `Basket TP set`/`Basket SL set` بههمراه فیلدها در لاگ دیده میشود (تا پیش از این فقط `ticket= | price=` ثبت میشد و TP از SL قابل تشخیص نبود).
- تستها: `test_set_basket_targets_local_tp_for_position_beyond_target` → `test_set_basket_targets_identical_tp_for_all_buys` (هر دو خرید تیپی مشترک 4006.0)؛ `test_set_basket_targets_sell_mirror` (هر دو فروش تیپی مشترک 3994.0).
- سیاست بازمحاسبه طبق انتخاب کاربر همان قبلی است: با هر پر شدنِ پوزیشنِ خلافجهت، هدف بازمحاسبه میشود (نوسان هدف از قصد باقی میماند؛ فقط دیگر پوزیشنهای همجهت تیپی متفاوت ندارند).

## تغییر بعدی v2: لنگر تیپیِ مشترک به بالای گرید
از تحلیل سیکل ۲ آخرین اجرا (۱۰:۴۳–۱۰:۴۷، anchor=4166.52): بعد از قفل، تیپیِ پوزیشنهای باقیمانده همچنان جابجا میشد (4168.01 ← 4168.11 ← 4168.25 ← 4168.57 ← 4168.78 ← 4169.11) بدون هیچ بازمحاسبه. علت: کلمپِ دوباره در `_set_basket_targets` — با هر پول، `eff_target = max(target, عمیقترین ورودِ همجهت + step)` دوباره از روی ورودهای فعلی محاسبه میشد؛ با پر شدنِ خریدِ عمیقتر بالا میرفت و با تیپیِ عمیقترین پوزیشن پایین میآمد. خریدهای C2_17..C2_20 تیپی 4169.11 بالای بازار گرفتند و با بستن دستی تمام شد (MT5 هم با 10013/10036 بعضی تنظیمها را رد کرد).

طبق تصمیم کاربر: «لنگر تیپی به بالای گرید» — تیپیِ مشترک به بالای گریدِ کاشتهشده لنگر میشود و فقط با رشدِ گرید (سفارشهای جدید) بالا میرود؛ پر شدنِ پوزیشن یا تیپیِ آن هرگز تیپی باقیماندهها را تغییر نمیدهد.

تغییر اعمالشده:
- `strategy/cycle_manager.py`:
  - `_set_basket_targets` — کلمپِ دوباره از روی عمیقترین ورود حذف شد؛ همهٔ پوزیشنهای همجهت دقیقاً همان `target_price`ِ دادهشده را میگیرند (فقط `_align_to_tick`).
  - `_reanchor_to_grid_top` — جدید: خرید غالب → `max(target, بالای گریدِ کاشتهشده + step)`؛ فروش غالب → `min(target, کف گریدِ کاشتهشده − step)` (مونوتونیک). از آنجا که پوزیشنها فقط از سطحهای گریدِ کاشتهشده باز میشوند، تیپیِ لنگرشده همیشه بالاتر از ورودِ هر پوزیشنِ همجهت است (رد شدن MT5 از بین میرود).
  - `_try_set_basket_target` — در مسیر قفل (فریز) و مسیر محاسبه، بعد از تعیین جهتِ سبد `_reanchor_to_grid_top` اجرا میشود تا تیپیِ ذخیرهشده همیشه به بالای گرید فعلی لنگر باشد؛ رشدِ گرید (افزایش `placed_buy_depth`/`placed_sell_depth`) تنها چیزی است که تیپی را جابجا میکند.
- تستها: `test_set_basket_targets_identical_tp_for_all_buys` و `test_set_basket_targets_sell_mirror` بهروز شدند (`_set_basket_targets` حالا همان هدفِ دادهشده را اعمال میکند). سه تست جدید: لنگر به بالای گرید هنگام قفل، بالا رفتنِ تیپی فقط با رشدِ گرید (نه با پر شدنِ همجهتِ عمیقتر)، و ثابت ماندنِ تیپیِ باقیماندهها بعد از تیپیِ همجهت (با بررسی درخواستهای modify واقعی).

## تغییر بعدی v2: رشدِ متقارنِ گرید (پندینگِ دوطرفه)
از گزارش کاربر: وقتی پوزیشنِ خلافجهت باز شد، تعدادش محدود ماند — بایها تا ۲۰ سطح کاشته شدند ولی سل فقط ۷. علت: `estimate_needed_depths` فقط سمتِ غالب را رشد میداد (`buy_count > sell_count` → `sell_depth = max(1, placed_sell_depth)`)، پس سمتِ مخالف هرگز با سمتِ در حالِ رشد همگام نمیشد.

طبق تصمیم کاربر: «هر وقت پوزیشنی در هر جهت پندینگ اضافه میشود باید در جهتِ مخالفش هم اضافه شود».

تغییر اعمالشده:
- `strategy/grid_builder.py` — `estimate_needed_depths` دیگر سمتِ غالب/غیرغالب را جدا نمیکند؛ یک عمقِ مشترک محاسبه و در هر دو جهت اعمال میکند:
  `depth = max(placed_buy_depth, placed_sell_depth, buy_count, sell_count, buy_base+1, sell_base+1)` (سقف `grid_count`). هر پول که گرید را رشد دهد، هر دو سمتِ buy/sell را به یک عمق میرساند.
- تستها: `test_estimate_needed_depths_mixed_sides` و `test_grow_grid_depth_uses_mixed_exposure` بهروز شدند تا رشدِ متقارن را تأیید کنند (دیگر «فقط سمتِ غالب رشد میکند» نیست).

## تغییر بعدی v2: رفع «ضرر خالص در هیستوریِ سیکلِ نیمهباز» (حذف کلمپِ تیپی به پایینِ سربهسر)
از گزارش کاربر: «در سیکلی که همهٔ معاملات هنوز باز نشدهاند، جمعِ خالصِ هیستوریِ معاملات ضرر شده؛ محاسبات غلطه». منبع دقیق پس از بررسی، هیستوریِ خودِ MT5 بود (نه داشبورد).

### علت ریشهای (تأییدشده با ریاضیات سیکل ۶)
در سبدِ درهم (مثلاً سیکل ۶: ۸ بای 4170.13–4170.83 + ۶ سل 4169.08–4169.58) تیپیِ هدفِ سبد توسط `calculate_basket_target` روی سربهسرِ واقعی سبد محاسبه میشود (~4179.91 برای رسیدن به `target_profit + کارمزدها`). اما `_keep_target_on_grid` این تیپی را به **بالای گریدِ کاشتهشده** (4172.03) کلمپ میکرد چون گرید نمیتوانست به هدفِ واقعی برسد:
- `target = last` در `cycle_manager._keep_target_on_grid` (بلاک 621-624 / 634-637).
- در 4172.03 سبدِ مذکور **−5.6** خالص میشد؛ وقتی تیپی زده میشد کل سیکل با ضرر بسته میشد و در هیستوریِ MT5 ثبت میشد.

### تغییر اعمالشده
- `strategy/cycle_manager.py` — `_keep_target_on_grid` دیگر تیپی را به پایین کلمپ نمیکند. گرید تا جای مجاز (`grid_count`) رشد میکند تا پندینگها در مسیر باز شوند، اما هدفِ ذخیرهشده همان مقدارِ سربهسرِ واقعی میماند (حتی اگر فراتر از گریدِ کاشتهشده باشد). قیمت باید همانقدر حرکت کند تا سیکل سودآور بسته شود.
- `_reanchor_to_grid_top` بعداً بازنویسی شد: بهجای لنگر به بالای گریدِ کاشتهشده، به بالای «پوزیشنِ بازِ همجهتِ فعلی» لنگر میشود (تغییرِ بعدی، بخشِ «TP واقعی» را ببینید). فقط «بالا بردن» با `max`/`min` است و هرگز هدف را به زیر سربهسر نمیآورد.
- تست: `test_keep_target_on_grid_never_clamps_below_break_even` جایگزینِ `test_keep_target_on_grid_clamps_beyond_grid` شد (هدفِ فراتر از گرید حفظ میشود؛ فقط گرید رشد میکند).

### وضعیت
- کل تستها: `85 passed`.
- شبیهسازی سیکل ۶: هدفِ نهایی 4179.91 (سربهسرِ واقعی) و خالص در هدف = 10.0 (مثبت) — دیگر کلمپِ ضررده وجود ندارد.

## تغییر بعدی v2: رد کردن پندینگِ نامعتبر هنگام کاشت (10015) بدون ریست کل سیکل
از گزارش کاربر و لاگ ۱۲:۳۴: «فقط دو تا پوزیشن در یک جهت کاشته شد و گرید درست ساخته نشد».

### علت ریشهای
`build_grid_prices` قیمتهای بای/سل را یک‌بار در زمانِ anchor می‌سازد و `_place_grid_depths` آن‌ها را به‌صورت پشت‌سرهم و با فاصلهٔ چند ثانیه‌ای می‌فرستد. اگر بازار در همین بازه جابجا شود (مثلاً صعودِ XAUUSD)، یک بای استاپ که موقعِ ساخت معتبر بود، هنگامِ ارسال به «خیلی نزدیک به ask فعلی» تبدیل می‌شود و MT5 با **10015 Invalid price** ردش می‌کند. رفتار قبلی هر شکستی را «کشنده» می‌دانست: همهٔ سفارش‌های موفق را حذف و `False` برمی‌گرداند → کل سیکل ریست می‌شد و دوباره از نو شروع می‌کرد (در لاگ سیکل‌های ۱ تا ۳ همین الگو تکرار شد؛ مثلاً بای C2_3 با قیمت 4164.33 رد شد و ۲ بایِ موفق هم حذف شدند).

### تغییر اعمالشده
- `strategy/cycle_manager.py` — `_place_grid_depths`:
  - retcodeهای **10015 (Invalid price)** و **10030 (Invalid fill)** به‌عنوان «رد شدنِ سطح» در نظر گرفته می‌شوند: لاگِ warning می‌گیرند و حلقه به سطح بعدی ادامه می‌دهد — بدون حذفِ سفارش‌های قبلی و بدون ریستِ سیکل.
  - فقط خطاهای **غیرقیمتی** (بقیهٔ retcodeها) کشنده‌اند و مثل قبل رولبک می‌کنند.
  - `placed_buy_depth`/`placed_sell_depth` از روی **سطحِ موفقِ واقعی** (grid_numberِ آخرین سفارشِ کاشته‌شده) به‌روز می‌شوند، نه از `buy_depth`/`sell_depth`ِ درخواستی. در نتیجه گریدِ ناقص باقی می‌ماند و بعداً توسط رشدِ گرید تکمیل می‌شود.
- طبق تصمیم کاربر: سطحِ نامعتبر «نادیده گرفته می‌شود» (قیمت جابجا نمی‌شود) و سیکل «با سطوحِ موفق ادامه می‌دهد» (ریست نمی‌شود).

### تست‌های جدید
- `test_place_grid_depths_skips_invalid_price_and_continues` — سطح 10015 رد و بقیه کاشته می‌شوند؛ رولبک اتفاق نمی‌افتد.
- `test_place_grid_depths_fatal_error_rolls_back` — خطای غیرقیمتی (10018) مثل قبل رولبک و `False` می‌دهد.

### وضعیت
- کل تست‌ها: `87 passed`.
- شبیه‌سازی سیکل ۲: بایِ سطح 3 رد شد ولی سطح‌های 1، 2، 4–7 و همهٔ سل‌ها کاشته شدند؛ `placed_buy_depth=7` و بدون ریست.

## تغییر بعدی v2: بستن خودکار سیکل وقتی هر دو طرف گرید کاملاً باز شدند + رفع بستن زودهنگام

### علت ریشهای (دو مشکل)
بر اساس گزارش کاربر: «وقتی همه پوزیشن‌های دو جهت باز شوند، کل پوزیشن‌ها باید بسته بشوند؛ اما الان کار نمیکند».

مشکل اول (تعریف قانون): قانون قبلی بر اساس `placed_buy_depth`/`placed_sell_depth` (عمق کاشتها) بود، ولی کاربر به **پوزیشنهای بازِ واقعی** در هر دو جهت اشاره دارد. گرید ممکن است تا سقف کاشته شود ولی هنوز پوزیشنی پر نشده باشد؛ در آن حالت نباید سیکل بسته شود.

مشکل دوم (باگِ بستن زودهنگام): `_check_all_positions_closed` فقط `len(positions) == 0` را چک میکرد. در لحظه‌ای که گرید کاشته شده ولی هنوز هیچ پوزیشنی باز نشده، لیست پوزیشن خالی است → تابع به اشتباه True برمی‌گرداند → سیکل قبل از باز شدن حتی یک پوزیشن، بسته و ریست میشد (در لاگ 09:53:09: `All positions closed | cycle=0` بلافاصله بعد از `Order placed` دیده شد). این باعث میشد پوزیشن‌های قدیمی (مانند `ticket=77 C1_1`) به سیکل جدید لوث شوند و قانون «هر دو طرف کامل» به‌درستی عمل نکند.

### تغییر اعمالشده
- `models/symbol_context.py` — فیلد جدید `both_sides_full_closed: bool = False` (برای جلوگیري از تکرار چندباره).
- `strategy/cycle_manager.py`:
  - `_both_sides_positions_full(ctx, sc)` — جدید: `True` وقتی **پوزیشن‌های بازِ واقعی** در هر دو جهت به سقف برسند (`buy_count >= grid_count` و `sell_count >= grid_count` و هر دو > 0).
  - `_check_both_sides_full_and_close(ctx, sc, sm)` — جدید: اگر هر دو طرف کامل شد و قبلاً انجام نشده، `both_sides_full_closed = True` کرده و به `SymbolState.CLOSING` منتقل میشود (یک‌بار در هر سیکل).
  - در `_handle_positions_active`، `_handle_target_active` و `_handle_locked_exposure`: این قانون قبل از `is_locked_exposure` چک میشود.
  - **`_check_all_positions_closed` اصلاح شد:** دیگر فقط `len(positions) == 0` نیست؛ `True` فقط وقتی که `position_tickets_seen` غیرخالی باشد و هیچ تیکتِ دیده‌شده در لیست پوزیشن‌های باز نباشد. این از بسته‌شدن زودهنگام سیکل هنگام کاشتِ گرید (بدون پوزیشن باز) جلوگیری میکند.
  - `_handle_resetting` — `both_sides_full_closed = False` ریست میشود.

### تست‌های جدید
- `test_both_sides_grid_full_triggers_closing` — وقتی هر دو طرف گرید کامل باز شوند (20 بای + 20 سل)، به CLOSING منتقل میشود.
- `test_both_sides_grid_full_not_triggered_when_one_side_short` — وقتی فقط یک طرف کامل باشد، قانون فعال نمیشود.
- `test_all_positions_closed_requires_seen_tickets_to_be_gone` — سیکل فقط وقتی کامل میشود که تیکت‌های دیده‌شده واقعاً بسته شده باشند (نه فقط لیست پوزیشن خالی).

### وضعیت
- کل تست‌ها: `93 passed`.
- شبیه‌سازی: وقتی 40 پوزیشن (20 بای + 20 سل) باز باشند، قانون به CLOSING میرود، هر 40 پوزیشن بسته میشوند و سیکل RESET میشود.

## تغییر بعدی v2: TP به پوزیشنهای باز و نه به بالای گریدِ عمیقِ کاشتهشده

از گزارش کاربر: «محاسبات تیپی غلطه؛ برای ۰.۵ دلار سود تیپی را خیلی دور برد».

### علت ریشهای (تأیید با اعدادِ لاگ)
`_reanchor_to_grid_top` هدفِ تیپی را روی `top + step` میگذاشت که `top = buy_grid_prices[placed_buy_depth - 1]` است — یعنی **بالای کل گریدِ کاشتهشده** (تا عمق ۲۰). چون گرید جلوتر از پوزیشنهای واقعی کاشته میشود، `top` خیلی بالاتر از سربهسرِ واقعیِ سبدِ هدفِ ۰.۵ دلاری میرفت. مثالِ تأییدشده: با ۱۸–۲۰ بای باید TP≈4159.26 باشد ولی به 4159.10–4162.70 افتاده بود (در حالی که برای ۰.۵ دلار فقط کمی بالاتر از سربهسر لازم است).

طبق راهنمای کاربر: «قرار بود بر اساس تیپی، تعداد پوزیشنها تعیین و کاشته شود» — یعنی TP باید از سربهسرِ واقعیِ پوزیشنها تبعیت کند، نه اینکه به بالای گریدِ دلخواهِ کاشتهشده چسبانده شود.

### تغییر اعمالشده
- `strategy/cycle_manager.py` — سینگنیچر `_reanchor_to_grid_top` به `(ctx, sc, positions)` تغییر کرد و لنگر را فقط به بالای **عمیقترین ورودِ پوزیشنِ بازِ همجهتِ فعلی** میبندد:
  - بای-غالب: `top_entry = max(price_open بایهای باز)`؛ تیپی = `max(target, top_entry + step)`.
  - فروش-غالب: `bottom_entry = min(price_open فروشهای باز)`؛ تیپی = `min(target, bottom_entry - step)`.
  - به این ترتیب تیپی همیشه بالای عمیقترین ورودِ باز میماند (MT5 رد نمیکند) ولی به سطحِ کاشتهشدهٔ هنوزِ پرنشده چسبیده نمیشود → برای ۰.۵ دلار نزدیک و واقعی است.
- `_keep_target_on_grid` فقط گرید را در جهتِ تیپی رشد میدهد (بدون تحریفِ تیپی) — عمقِ گرید همچنان از روی تیپی/هدف بزرگ میشود تا رسیدن به آن در محدودهٔ کاشتهشده ممکن باشد.

### تستهای بهروزشده/جدید
- `test_basket_target_anchored_to_deepest_open_buy_on_lock` — تیپی به بالای عمیقترین بایِ باز (4003)+step = 4004، نه بالای گرید.
- `test_basket_target_not_anchored_to_deep_planted_grid` — گرید تا ۲۰ سطح کاشته میشود ولی تیپی همچنان ≈4004 میماند (نه ~4021). این دقیقاً گیرِ گزارشِ کاربر را بازتولید میکند.
- `test_basket_target_raises_when_deeper_open_position_fills` — با باز شدنِ بایِ واقعیِ عمیقتر (4004)، تیپی به 4005 بالا میرود (بالای ورودِ جدید).
- `test_basket_target_remaining_tp_identical_and_frozen_on_tp_out` به 4004 بهروز شد.

### وضعیتِ مشکل TP
- شبیهسازی با دادهٔ واقعیِ لاگ: تیپی = 4157.69 (بالای عمیقترین ورودِ باز 4157.58 + 0.11) بهجای پرش به بالای گریدِ عمیق (4159+). سودِ ۰.۵ دلاری با تیپیِ نزدیک واقعی.

## تغییر بعدی v2: بستنِ کاملِ پوزیشنها هنگام Ctrl+C (هاردن _close_all_positions)
از گزارش کاربر: «وقتی Ctrl+C میزنم باید همهٔ پوزیشنها را دستی ببندد ولی نمیشود».

### علت/ضعف
`_close_all_positions` فقط retcodeهای `DONE`/`DONE_PARTIAL` را موفق میدانست. اگر MT5 با retcode متفاوتی پاسخ دهد (مثلاً 10027 «پوزیشن قبلاً بسته شده» یا خطای موقت/سرور)، پوزیشن نابسته میماند. `_handle_closing` نیز فقط چند بار تکرار میکند و اگر همچنان پوزیشنی مانده باشد، کاربر باید دوباره Ctrl+C بزند (یا با خاموشیِ ربات پوزیشنها باز بمانند).

### تغییر اعمالشده
- `strategy/cycle_manager.py`:
  - ثابت `MT5_RETCODE_POSITION_CLOSED = 10027` اضافه شد.
  - `_close_all_positions` retcodeهای موفقِ **10009 (DONE)**، **10010 (DONE_PARTIAL)** و **10027 (POSITION_CLOSED)** را میپذیرد.
  - بعد از صرفِ همهٔ تلاشها برای هر پوزیشن، یک **بررسی مجددِ MT5** انجام میدهد: اگر تیکتِ پوزیشن دیگر در `get_open_positions` نیست، آن را «بستهٔ تأییدشده» میداند (برای مواردی که سرور پوزیشن را بسته ولی retcodeِ درخواست متفاوت بوده است).

### تست جدید
- `test_close_all_positions_accepts_position_closed_retcode` — retcode 10027 (بستهٔ سمتِ سرور) موفق تلقی میشود.

### وضعیتِ کلی تستها
- کل تستها: `89 passed`.

## نکته مهم (از تحلیل لاگ v1)
نمونه در حال اجرا در ۲۰۲۶-۰۸-۰۵ از کد کامیتشده قدیمیتر بود (فرمت لاگهای تنظیم تیپی و «Cycle closed» مطابقت نداشت). قبل از قضاوت روی رفتار جدید، حتماً از کد همین پوشه اجرا کنید.

## تست
- `python -m pytest tests/ -q` → `93 passed` (پایه + فریز هدف + لنگر گرید + نکلمپِ زیر سربهسر + ردِ پندینگِ نامعتبر + لنگرِ تیپی به ورودِ باز + هاردنِ بستنِ پوزیشن + قانون بستن خودکار هنگام کامل شدنِ هر دو طرف + رفع بستن زودهنگام).
- تستهای جدید: ردیابی تیکتهای دیدهشده، تشخیص کاملشدن، عدم تکمیل وقتی هنوز پوزیشن باز است، و رفتار هندلر TARGET_ACTIVE (بسته نشدن با پوزیشن باز + کامل شدن بعد از بستهشدن همه).
- تستهای جدیدِ فریز هدف: فریز با بستهشدنِ همراستا، فریز با پر شدنِ همراستای جدید، بازنگری با باز شدنِ پوزیشن خلافجهت، چرخشِ جهت با هجِ بزرگتر، پاکسازی در ریست، و جمعِ یکبارِ `realized_net_profit`.

## اجرا
- تنظیم: `config.yaml` یا `data/user_defaults.yaml` (دومی از git حذف است).
- حالت تست: `dry_run: true` یا `--dry-run`.
- حساب Hedging لازم است.

## Git
- این پوشه بدون `.git` کپی شده است (هیستوری و ریموت ندارد). برای اتصال به گیتهااب یا ساخت رپوی جدا، از `git init` استفاده کنید.
